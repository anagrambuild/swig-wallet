[package]
name = "slippage-oracle"
version = "0.1.0"
edition = "2021"

[dependencies]
pinocchio = "0.9"
pinocchio-pubkey = "0.3"
swig-assertions = { path = "../assertions" }

[lib]
crate-type = ["cdylib", "lib"]

[dev-dependencies]
litesvm = "0.6.1"
solana-sdk = "2"

[features]
no-entrypoint = []
